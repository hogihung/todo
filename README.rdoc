== README

* Ruby version -> 2.1.2

* Rails version -> 4.1.4

* System dependencies -> Rspec, Factory Girl, Capyabara

* Configuration - see the Gemfile, be sure to bundle

* Database creation -> create your own config/database.yml (or equivalent)

* Database initialization -> rake db:migrate

* How to run the test suite -> Type rspec to run the full suite
